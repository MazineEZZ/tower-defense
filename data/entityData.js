const turretData = {
  base: { startX: 0, startY: 0, spriteWidth: 32, spriteHeight: 32 },
  head: { startX: 32, startY: 0, spriteWidth: 32, spriteHeight: 32 },
};

const zombieData = {
  width: 32,
  height: 15,
};

export { turretData, zombieData };
